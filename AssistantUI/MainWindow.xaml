<Window x:Class="AssistantUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AssistantUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🎙️ Голосовой ассистент Domovoy" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="5"/>

        <GroupBox Header="Распознанная речь" Grid.Row="1" Margin="5">
            <ScrollViewer>
                <TextBox x:Name="txtRecognized" TextWrapping="Wrap" IsReadOnly="True" FontSize="16" MinHeight="100"/>
            </ScrollViewer>
        </GroupBox>

        <GroupBox Header="Лог команд" Grid.Row="2" Margin="5">
            <ScrollViewer>
                <ListBox x:Name="lstLog" Height="120"/>
            </ScrollViewer>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button x:Name="btnOn" Content="Включить свет" Width="120" Margin="5" Click="BtnOn_Click"/>
            <Button x:Name="btnOff" Content="Выключить свет" Width="120" Margin="5" Click="BtnOff_Click"/>
            <Button x:Name="btnToggle" Content="Переключить" Width="120" Margin="5" Click="BtnToggle_Click"/>
            <Button x:Name="btnStatus" Content="Статус" Width="80" Margin="5" Click="BtnStatus_Click"/>
        </StackPanel>
    </Grid>
</Window>
